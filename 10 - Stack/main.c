#include <stdio.h>
#include <stdlib.h>

// Implemented mostly using array or linked list
// Pointer to top most element
// Operations - pop(), push(value), peek(), isEmpty(), isFull(), valueAtIndex(index)
// LIFO principle
int main()
{

    return 0;
}
